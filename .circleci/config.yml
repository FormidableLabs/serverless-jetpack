version: 2.1

orbs:
  node: circleci/node@1.1

jobs:
  build:
    executor:
      name: node/default
      tag: '10'

    steps:
      - checkout

      - node/with-cache:
          steps:
            - run: yarn install --frozen-lockfile

      - run: node --version
      - run: pwd
